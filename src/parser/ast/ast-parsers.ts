import { AstNodeType } from './ast'
import { AstParser } from './ast-parser'

// export const binaryOperator = AstParser.new(
    //     (from, to) => ({ type: AstNodeType.BinaryOperator }),
    //     AstParser.new()
// )

export const binaryOperator = AstParser.new(
    //     (from, to) => ({ type: AstNodeType.BinaryOperator }),
    //     AstParser.new()
// )

